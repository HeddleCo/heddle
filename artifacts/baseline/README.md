# Baseline artifacts

Machine-readable program baselines. Prefer committing:

- `wave0-merged/summary.json` (and future `*/summary.json`)
- `environment` snapshots when certifying

Do **not** commit large per-job `logs/*.log` files unless investigating a failure.
