[package]
name = "capability-verifier-conformance-native"
version = "0.0.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
heddleco-capability-verifier = { path = "../.." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
